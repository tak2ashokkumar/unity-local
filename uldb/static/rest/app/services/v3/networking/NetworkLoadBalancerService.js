var app = angular.module('uldb');
app.factory('NetworkLoadBalancerService', [
    '$timeout',
    '$http',
    'RestService',
    'AdminApi',
    function ($timeout,
              $http,
              RestService,
              AdminApi) {
        return {};
    }
]);
